# Local development defaults
PORT=3001
FRONTEND_URL=http://localhost:5173
NODE_ENV=development

# JWT secret for local dev only
JWT_SECRET=lingjing_secret_dev

# Optional: enable AI endpoints
# DASHSCOPE_API_KEY=

# Optional: mail settings (only needed if using email features locally)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
